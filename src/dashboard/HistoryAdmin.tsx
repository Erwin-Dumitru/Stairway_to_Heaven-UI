import "./styles/HistoryAdmin.css"

function HistoryAdmin({currentAddress}: {currentAddress: string}) {
    return (
        <div className="historyAdmin">
            <h1>History admin</h1>
        </div>
    );
}

export default HistoryAdmin;
