import "./styles/ChatAdmin.css"

function ChatAdmin({currentAddress}: {currentAddress: string}) {
    return (
        <div className="chatAdmin">
            <h1>Chat admin</h1>
        </div>
    );
}

export default ChatAdmin;
